---
- name: Enable the synchronized offline Debian mirror
  hosts: burner
  become: true
  gather_facts: true
  vars_files:
    - - local.yml
      - local.yml.example
  roles:
    - role: offline_mirror
      tasks_from: enable
