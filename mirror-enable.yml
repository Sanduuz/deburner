---
- name: Enable the synchronized offline Debian mirror
  hosts: burner
  become: true
  gather_facts: true
  vars_files:
    - - local.yml
      - local.yml.example
  tasks:
    - name: Point APT at the synchronized offline mirror
      ansible.builtin.include_role:
        name: offline_mirror
        tasks_from: enable
